// Ambient type declarations for modules that are only available at runtime
// in a Nuxt application context.
//
// The nuxt/app module shim lives in src/types/nuxt-app-shim.d.ts so that
// vite-plugin-dts (which scans src/**/*.ts) picks it up during declaration
// file generation.
